# スプレッドシート画像自動コメント生成ツール

スプレッドシートに貼り付けた指摘事項の画像を、Claude AI が自動で分析しコメントを生成します。

## セットアップ手順

### 1. Anthropic APIキーの取得

1. [Anthropic Console](https://console.anthropic.com/) にアクセス
2. アカウントを作成またはログイン
3. APIキーを発行

### 2. Google Apps Script の設定

1. 対象のスプレッドシートを開く
2. メニューの **「拡張機能」→「Apps Script」** をクリック
3. エディタが開いたら、既存のコードを全て削除
4. `auto_comment_from_images.gs` の内容を貼り付け
5. **「保存」** ボタンをクリック

### 3. APIキーの登録

1. スプレッドシートをリロード（メニューが追加されます）
2. メニュー **「画像コメント自動生成」→「APIキーを設定」** をクリック
3. 取得したAnthropic APIキーを入力

### 4. 設定のカスタマイズ

スクリプト冒頭の `CONFIG` を自分のスプレッドシートの構造に合わせて変更してください：

```javascript
const CONFIG = {
  IMAGE_COLUMN: 4,        // 画像が貼られている列（A=1, B=2, C=3, D=4...）
  COMMENT_COLUMN: 5,      // コメントを書き込む列
  DATA_START_ROW: 2,      // データの開始行（ヘッダー行の次）
  SHEET_NAME: "",          // 対象シート名（空=アクティブシート）
  ITEM_NAME_COLUMN: 2,    // 指摘項目名の列（AIに渡すコンテキスト）
  LOCATION_COLUMN: 3,     // 指摘箇所の列（AIに渡すコンテキスト）
  OVERWRITE_EXISTING: false, // 既存コメントの上書き
};
```

## 使い方

### 選択範囲のコメント生成
1. コメントを生成したい行を選択
2. メニュー **「画像コメント自動生成」→「選択範囲のコメントを生成」**

### シート全体のコメント生成
1. メニュー **「画像コメント自動生成」→「シート全体のコメントを生成」**

## 画像の配置方法

以下の形式に対応しています（推奨順）：

1. **OverGridImage（推奨）**: セルの上に画像を配置（挿入→画像→セル内の画像）
2. **=IMAGE()関数**: `=IMAGE("画像URL")` で表示された画像
3. **URLテキスト**: セルに画像のURLを直接入力

### 推奨：OverGridImage

スプレッドシートで **「挿入」→「画像」→「セル内の画像」** を使用して
画像列（CONFIG.IMAGE_COLUMN）のセルに画像を挿入してください。
この方式が最も確実に動作します。

## AIプロンプトのカスタマイズ

`CONFIG.SYSTEM_PROMPT` を変更することで、AIの応答スタイルを調整できます。
例えば、特定の業界用語を使用させたり、チェックリスト形式で出力させたりできます。

## 注意事項

- Anthropic API の利用には料金が発生します
- 画像1枚あたり約1秒のウェイトを入れています（API制限対策）
- 大量の画像を処理する場合、Google Apps Script の実行時間制限（6分）に注意してください
- 機密性の高い画像はAPIに送信されるため、セキュリティポリシーを確認してください
