# スプレッドシート画像自動コメント生成ツール

【焼肉きんぐ甲府飯田】57期臨店報告書のスプレッドシートで、問題写真をClaude AIが自動で分析しO列にコメントを生成します。

## 対象シート

- **営業前インスペ**
- **営業中インスペ**

## 列構成

| 列 | 内容 | 用途 |
|----|------|------|
| G | NO | 項番 |
| H | 分類 | AIにコンテキストとして渡す |
| I | 項目 | AIにコンテキストとして渡す |
| J | 今回 | チェックボックス（未チェック=指摘あり→処理対象） |
| K | 前回 | - |
| L | 問題写真 | AIが分析する画像（1枚目） |
| M | 問題写真 | AIが分析する画像（2枚目） |
| N | 改善内容 | AIにコンテキストとして渡す |
| **O** | **コメント** | **AIが自動生成して書き込む列** |

## セットアップ手順

### 1. Anthropic APIキーの取得

1. [Anthropic Console](https://console.anthropic.com/) にアクセス
2. アカウントを作成またはログイン
3. APIキーを発行

### 2. Google Apps Script の設定

1. 対象のスプレッドシートを開く
2. メニューの **「拡張機能」→「Apps Script」** をクリック
3. エディタが開いたら、既存のコードを全て削除
4. `auto_comment_from_images.gs` の内容を貼り付け
5. **「保存」** ボタンをクリック

### 3. APIキーの登録

1. スプレッドシートをリロード（メニューが追加されます）
2. メニュー **「画像コメント自動生成」→「APIキーを設定」** をクリック
3. 取得したAnthropic APIキーを入力

## 使い方

### 現在のシートのコメント生成
1. 営業前インスペ or 営業中インスペを開く
2. メニュー **「画像コメント自動生成」→「現在のシートのコメントを生成」**

### 選択範囲のコメント生成
1. コメントを生成したい行を選択
2. メニュー **「画像コメント自動生成」→「選択範囲のコメントを生成」**

### 全シート一括生成
1. メニュー **「画像コメント自動生成」→「全対象シートのコメントを生成」**

## 処理の流れ

1. J列（今回）が **未チェックの行** のみ処理
2. L列・M列から **問題写真** を取得
3. H列（分類）・I列（項目）・N列（改善内容）をコンテキストとしてAIに渡す
4. Claude APIが画像を分析し、コメントを生成
5. **O列にコメントを書き込み**

## 画像の配置方法

L列・M列に以下の形式で画像を配置できます（推奨順）：

1. **セル上の画像（推奨）**: 「挿入」→「画像」→「セル内の画像」
2. **=IMAGE()関数**: `=IMAGE("画像URL")`
3. **URLテキスト**: セルに画像URLを直接入力

## 注意事項

- Anthropic API の利用には料金が発生します（画像分析1回あたり約数円）
- 画像1枚あたり約1秒のウェイトを入れています（API制限対策）
- Google Apps Script の実行時間制限は6分です（大量行の場合は「選択範囲」で分割実行推奨）
- 既にO列にコメントがある行はスキップされます（上書きしたい場合は`CONFIG.OVERWRITE_EXISTING`をtrueに変更）
